export interface Stat {
    _id: string;
    createdAt: string;
    updatedAt: string;
    count: string; // Assuming count is a string based on your example
    label: string;
    imageUrl: string;
    active: boolean;
    __v: number;
  }